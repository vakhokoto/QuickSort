To run program open up terminal in Linux based systems at the file's directory and run following command
    make
after that feed the data required and program will output the answer at output.txt file